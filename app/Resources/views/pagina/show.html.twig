{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pagina</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pagina.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ pagina.titulo }}</td>
            </tr>
            <tr>
                <th>Contenido</th>
                <td>{{ pagina.contenido }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{% if pagina.createdAt %}{{ pagina.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('paginas_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('paginas_edit', { 'id': pagina.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
